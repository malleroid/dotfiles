# Files removed from source that should also be deleted from target machines
# (chezmoi apply does not remove targets when their source disappears)
#
# This list is transient: it is evaluated on every apply, so drop an entry once every
# machine has applied it. Left in place, it silently deletes anything that later reuses
# the same path.
